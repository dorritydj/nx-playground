const names = ['Michael', 'Melissa', 'Andrea'];

// inserting, removing - depends
// searching O(n)
// accessing O(1)

names[2]; // O(1)

// inserting at the end is O(1)
// inserting at the beginning is O(n) due to re-indexing the array
// same goes for removing

// push O(1)
// pop O(1)
// shift O(N)
// unshift O(N)
// concat O(N)
// slice O(N)
// sort O(N log N)
// forEach/map/filter/reduce/etc O(N)
